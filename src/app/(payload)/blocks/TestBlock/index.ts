import { Block } from "payload";

const TestBlock: Block = {
    slug: 'testBlock',
    fields: [
        {
            name: 'test',
            type: 'checkbox'
        },
    ]
}

export default TestBlock;